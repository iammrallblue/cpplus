#include <iostream>

using namespace std;

/*
    Reserved Words in C++:
	asm, inline, typeid, dynamic_cast, typedef, typename, signed, 
	sizeof, union, mutable, unsigned, using, namespace, explicit,
	static_cast, export, struct, extern, virtual, const_cast, void,
	volatile, wchar_t, register, reinterpret_cast, goto, delete, 
	operator, template, this,

    Key points:
        1. DO NOT use keyword as identifiers, they are reserved in C++ Library 
*/

int main()
{
    int int_number = 10;
    // int int = 10 // error
    return 0;
}