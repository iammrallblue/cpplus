#include <iostream>

using namespace std;

/*
    Variables:
		A variable is a memory location, with a name and a type, 
		that stores different values in each moment of a program’s execution. 

		Syntax:
			variable:
				int a;
			value:
				int a = 10;
			address:
				&a = 0xe01011
				
		Name of Variables (Identifiers):
			1. avoid duplicated names
			2. start with letter, or _
			3. Identifiers only can contain _, letters, and numbers
			4. can not use keyword (reserved words)
			5. correct variables:
				radio, Radio, Radio_Frequency, H2o, 
				the_beat_running, WoodStock, etc.

			6. better use two or more words combination.
*/

int main()
{

    // typeof unsigned Datatypes
    size_t var = 19;
    string str = "Bella,";
    return 0;
}